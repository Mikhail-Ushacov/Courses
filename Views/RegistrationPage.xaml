<Page x:Class="Courses.RegistrationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Registration Page">
    <Grid>
        <ListView ItemsSource="{Binding AvailableCourses}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Course Name" DisplayMemberBinding="{Binding CourseName}" />
                    <GridViewColumn Header="Instructor" DisplayMemberBinding="{Binding Instructor}" />
                </GridView>
            </ListView.View>
        </ListView>
        
        <Button Content="Register" Command="{Binding RegisterCommand}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10"/>
    </Grid>
</Page>
