<Page x:Class="Courses.CourseRegistrationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="Реєстрація на курси">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ui:Button Click="BackButton_Click"
                       Appearance="Transparent"
                       Padding="8"
                       Margin="0,0,16,0">
                <ui:SymbolIcon Symbol="ArrowLeft24" FontSize="20"/>
            </ui:Button>

            <TextBlock Grid.Column="1"
                       Text="Доступні курси"
                       FontSize="28"
                       FontWeight="Bold"
                       VerticalAlignment="Center"/>
        </Grid>

        <TextBlock Grid.Row="1"
                   Text="Оберіть курс для реєстрації"
                   FontSize="14"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   Margin="0,8,0,16"/>

        <ui:ListView Grid.Row="2"
                     ItemsSource="{Binding AvailableCourses}"
                     SelectedItem="{Binding SelectedCourse, Mode=TwoWay}"
                     SelectionMode="Single">
            <ui:ListView.ItemTemplate>
                <DataTemplate>
                    <ui:CardAction Margin="0,4" Padding="16" Cursor="Hand">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="Book24" FontSize="24" Margin="0,0,16,0"/>
                                <StackPanel>
                                    <TextBlock Text="{Binding CourseName}"
                                               FontSize="16"
                                               FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Instructor, StringFormat='Викладач: {0}'}"
                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                               FontSize="13"
                                               Margin="0,4,0,0"/>
                                </StackPanel>
                            </StackPanel>

                            <ui:Button Grid.Column="1"
                                       Content="Зареєструватися"
                                       Command="{Binding DataContext.RegisterCommand, RelativeSource={RelativeSource AncestorType=ui:ListView}}"
                                       CommandParameter="{Binding}"
                                       Appearance="Primary"
                                       Padding="16,8"
                                       Margin="16,0,0,0"/>
                        </Grid>
                    </ui:CardAction>
                </DataTemplate>
            </ui:ListView.ItemTemplate>
        </ui:ListView>
    </Grid>
</Page>
