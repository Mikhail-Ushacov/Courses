<Page x:Class="Courses.TestPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel Margin="20">
        <TextBlock Text="{Binding TestTitle}" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <ItemsControl ItemsSource="{Binding Questions}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="0,10">
                        <TextBlock Text="{Binding QuestionText}" FontWeight="Bold"/>
                        <ItemsControl ItemsSource="{Binding AnswerOptions}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding AnswerText}"
                                              IsChecked="{Binding IsSelected, Mode=TwoWay}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Button Content="Завершити тест"
                Command="{Binding SubmitTestCommand}"
                Margin="0,20,0,0"/>
    </StackPanel>
</Page>
